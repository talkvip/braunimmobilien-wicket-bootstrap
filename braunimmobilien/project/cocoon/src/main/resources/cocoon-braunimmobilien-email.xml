<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
xsi:schemaLocation="http://www.springframework.org/schema/beans
   http://www.springframework.org/schema/beans/spring-beans-2.0.xsd">

  <!-- <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
      <property name="host" value="mail.csonth.gov.uk"/>
   </bean>

   <bean id="registrationService" class="com.foo.SimpleRegistrationService">
      <property name="mailSender" ref="mailSender"/>
      <property name="velocityEngine" ref="velocityEngine"/>
   </bean>-->
   
   <bean id="velocityEngine" class="org.springframework.ui.velocity.VelocityEngineFactoryBean">
    <property name="velocityProperties">
    <props>
      <prop key="resource.loader">file</prop>
      <prop key="file.resource.loader.class">
       ${file.resource.loader.class}
      </prop>
  <prop key="file.resource.loader.path">${file.resource.loader.path}</prop>
      <prop key="file.resource.loader.cache">false</prop>
    </props>
  </property>
   </bean>
 <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
    <property name="host" value="${email.host}" />
    <property name="username" value="${email.host.username}" />
    <property name="password" value="${email.host.password}" />
       <property name="port"><value>${email.host.port}</value></property>
       <property name="protocol"><value>${email.smtp.protocol}</value></property>
    <property name="javaMailProperties">
      <props>
        <prop key="mail.smtp.auth">${email.smtp.auth}</prop>
        <prop key="mail.smtp.starttls.enable">${email.smtp.starttls.enable}</prop>
         <prop key="mail.smtp.quitwait">${email.smtp.quitwait}</prop>
         <prop key="mail.debug">${email.debug}</prop>
      </props>
    </property>
  </bean>

  <!-- this is a template message that we can pre-load with default state -->
  <bean id="templateMessage" class="org.springframework.mail.SimpleMailMessage">
    <property name="from" value="${email.from}" />
    <!-- you can set default values -->
    <property name="subject" value="Testing" />
    <property name="to" value="${email.to}" />
    <!-- you can set default values -->
    <property name="text" value="Testing" />
  </bean>
</beans>
